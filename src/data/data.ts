import { IProduct, IFilter } from '../utils/interfaces';
import { products } from './products.json';
import { filters } from './filters.json';

export const productData: IProduct[] = products;
export const filterData: IFilter = filters;
